# Data

Папка для демо-данных и будущих seed-файлов.

Сюда можно складывать:

- `clients.json`;
- `cases.json`;
- `events.json`;
- `tasks.json`;
- `mailing_templates.json`;
- `finance_operations.json`.

Пока демо-данные находятся в `frontend/app.js`, чтобы прототип открывался без сервера.

