{
  "name": "advocates-crm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8000 -d frontend",
    "test": "npm run test:smoke",
    "test:smoke": "playwright test",
    "test:smoke:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
