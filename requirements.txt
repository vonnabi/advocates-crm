# Future Django backend dependencies.
# Install later inside a virtual environment:
# python3 -m venv .venv
# source .venv/bin/activate
# pip install -r requirements.txt

Django>=5.0,<6.0
dj-database-url>=2.1,<3.0
gunicorn>=22.0,<24.0
psycopg[binary]>=3.1,<4.0
python-dotenv>=1.0,<2.0
whitenoise[brotli]>=6.6,<7.0
celery>=5.3,<6.0
redis>=5.0,<6.0
requests>=2.31,<3.0
anthropic>=0.70
pypdf>=4.0
